#!/usr/bin/env fish
# qs-dev: Run quickshell with local config for development

set -l script_dir (dirname (dirname (status -f)))
set -l config_dir "$script_dir/config/quickshell"

exec quickshell -p "$config_dir" $argv
