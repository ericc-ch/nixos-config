---
name: maintain-verification-skill
description: "Audits and updates a project's verification skill and feature map against code churn. Use for /maintain-verification-skill, 'audit verify skill', or after major feature refactors. Skip if the repo has no verify skill yet."
---

# Maintain a Verification Skill

A feature map rots the moment the app changes. This skill is the upkeep loop for a project-local skill generated by `create-verification-skill`. Cover every feature file from source and exercise every feature live.

## Outcomes

Report one of these outcomes:
- **clean:** Every feature has source and live test coverage. Nothing needs changing.
- **changed:** Proven documentation, harness, or feature map fixes applied.
- **blocked:** Testing could not finish or a fix could not run safely. Report what blocked the run.

## Edit Scope

Only edit the verification skill directory (`.agents/skills/verify-<app>/`, its `features/` folder, and harness scripts). Never edit product code during this pass. If app behavior differs from the feature map, it is either documentation drift or a bug in the app. If it is documentation drift, update the feature map. If it is a bug, report it to the user. Do not hide bugs by changing the feature map.

## Steps

0. Find `.agents/skills/verify-*/`. If no verification skill exists, stop and recommend `create-verification-skill`.
1. Check `features/README.md` against sibling feature files. Remove missing, dead, or duplicate entries.
2. If subagents are available, spawn one read-only subagent per feature file. Each subagent inspects the feature source code, flags documentation drift with line citations, and returns a test recipe. Subagents must never edit files or drive the app.
3. Merge recipes into efficient execution flows. Check recent git commits for new user-facing features missing from the map.
4. Drive every feature live using the Launch and Drive instructions in the verification skill.
   - Run the Doctor check before driving.
   - Ensure captured evidence survives all cleanups.
   - Ensure no background processes or scratch state outlive the run.
   - Re-run any modified harness scripts before finishing.
5. Sort findings into three categories:
   - Wrong user description: Update the feature file.
   - Harness cannot drive working feature: Fix the harness script.
   - App behavior broken: Document the bug for the user.
6. Summarize test coverage, confirmed drift, and unreached features.
